const pdf = require('./index');

// pdf.hasPdfTk()
//     .then(console.log)
//     .catch(console.error)

// pdf.config({
//     binaryPath: '../../bin/pdftk',
// });


pdf
    // .config({
    //     binaryPath: '../../bin/pdftk',
    // })
    .input('../test.pdf')
    .fillForm({
        distnameHeading: 'Hello There',
    })
    .flatten()
    .output('./done.pdf')
    .then(() => console.log('done'))
    .catch(console.error);

pdf.input('../test.pdf')
    .cat()
    .output('./done2.pdf')
    .then(() => console.log('done 2'))
    .catch(console.error);

pdf.input('../test.pdf')
    .cat()
    .output('./done3.pdf')
    .then(() => console.log('done 3'))
    .catch(console.error);

pdf.input('../test.pdf')
    .cat()
    .output('./done4.pdf')
    .then(() => console.log('done 4'))
    .catch(console.error);
